// C:\dev\frontend\nartex\postcss.config.mjs
const config = {
  plugins: [
    require('tailwindcss'), // Correct plugin
    require('autoprefixer'), // Autoprefixer for better browser support
  ],
};

export default config;
